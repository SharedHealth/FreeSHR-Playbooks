{% if nagios_hosts is defined %}
{% for host in nagios_hosts %}
define host {
       use {{ host.use}}
       host_name {{ host.name }}
       alias {{ host.alias }}
       address {{ host.address}}
}
{% endfor %}
{% endif %}


