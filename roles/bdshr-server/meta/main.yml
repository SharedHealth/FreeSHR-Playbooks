---
dependencies:
  - { role: shr-tools}
  - { role: oracle-java }
  - { role: sharedhealth-user }
  - { role: host-entries, exclude: "shr" }
  - { role: cassandra-keyspace, cassandra_keyspace: "{{ bdshr_keyspace }}", cassandra_keyspace_user: "{{ bdshr_cassandra_user }}", cassandra_keyspace_pass: "{{ bdshr_cassandra_pass }}", cassandra_keyspace_replication_strategy: "{{ bdshr_cassandra_replication_strategy }}", cassandra_keyspace_replication_factor: "{{ bdshr_cassandra_replication_factor }}" }
