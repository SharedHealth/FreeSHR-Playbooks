---
logstash_repo_key: "http://packages.elasticsearch.org/GPG-KEY-elasticsearch"
logstash_yum_config: "/etc/yum.repos.d/logstash.repo"

log_files:
  - paths:
      - /var/log/messages
      - /var/log/auth.log
    fields:
      type: syslog
